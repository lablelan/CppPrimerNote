#include <iostream>

int main()
{
    std::string s;

    auto iter = s.begin();
    while (iter != s.end()) {}
    while (bool status = (s.find(word) >= 0)) {}
    if (!status) {}



    return 0;
}
